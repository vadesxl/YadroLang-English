# Multi-label taint: Credentials (env.secret) flow to network.
# Leaking API keys/tokens to external services.

fn leak_secret() requires [NetworkAccess] {
    let token = env.secret()
    return net.send(token)
}

fn main() {
    return 0
}
