# Regression on review Codex: chain of_assignments v loop.
# a=b; b=c; c=source(); sink(a) - after fixpoint's 'a' becomes_tainted,
# i analyzer Must catch leak. Two of passes few - needed fixpoint.

fn leak() requires [NetworkAccess] {
    let a = 0
    let b = 0
    let c = 0
    let i = 0
    while i < 5 {
        a = b
        b = c
        c = user.data()
        net.send(a)
        i = i + 1
    }
    return 0
}

fn main() { return 0 }
